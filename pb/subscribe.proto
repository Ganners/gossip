syntax = "proto3";

// When we subscribe or are subscribed to, we expect to receive this
// payload
message Subscribe {
  string name = 1;
  string description = 2;
  string host = 3;
  string port = 4;
}
